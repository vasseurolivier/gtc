
'use server';

// This file is no longer used for client-side uploads,
// but could be repurposed for server-to-server file operations if needed.
// The new upload logic is handled directly in `src/app/admin/layout.tsx`
// and `src/app/admin/products/page.tsx` using the client-side Firebase Storage SDK.
