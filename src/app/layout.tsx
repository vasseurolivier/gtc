// This file is intentionally blank.
// The root layout has been moved to src/app/[locale]/layout.tsx
// to accommodate the internationalization routing structure.
export default function RootLayout({ children }: { children: React.ReactNode }) {
    return children;
}
