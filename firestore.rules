rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * FIRESTORE SECURITY RULES - GLOBAL TRADING CHINA
     * Prototype mode: Full access allowed
     */

    match /{document=**} {
      allow read, write: if true;
    }
  }
}